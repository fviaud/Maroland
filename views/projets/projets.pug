extends ../layout.pug
block content
  div(class="col-sm-9")
    nav(aria-label="breadcrumb")
      ul(class="breadcrumb")
        li(class="breadcrumb-item active" aria-current="page") Projets
  nav(class="navbar")
    a(class="btn btn btn-outline-primary btn-sm ml-auto m-1" href="/projets/new")
      span New projet
  include projets-list.pug