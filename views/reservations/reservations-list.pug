div(class="table-responsive")
  table(class="table table-hover")
    thead
      tr
        th(scope="col") Nom
        th(scope="col") Last
        th(scope="col") Handle
    tbody
      each reservation in reservations
        tr
          td=reservation.nom
          td Otto
          td @mdo

